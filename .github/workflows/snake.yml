name: Generate Datas
on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:
  push:
    branches:
    - main

# Adicione este bloco para liberar a permissão de escrita
permissions:
  contents: write

jobs:
  build:
    name: Jobs to update datas
    runs-on: ubuntu-latest
    steps:
      - uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
        env:
          # Aqui é onde a mágica acontece. Trocamos o token padrão pelo seu token com permissão total.
          GITHUB_TOKEN: ${{ secrets.SNAKE_TOKEN }}
          
      - uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          # MANTENHA o token padrão aqui embaixo, ele é usado apenas para publicar a imagem.
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
